# Dev-codespace

```Bash
    python3 -m venv ~/.dev
    source ~/.dev/bin/activate

```

```Bash
    docker run -it noahgift/cloudapp python  app.py --name "Big John"
```

```Bash
    chmod +x build.sh
    chmod +x build.sh
```

```Bash
    docker run -it cloudapp python  app.py --name "Big John"
```